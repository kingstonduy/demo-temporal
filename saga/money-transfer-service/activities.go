package app

import (
	"context"
	shared "kingstonduy/demo-temporal/saga"
)

func ValidateAccount(ctx context.Context, input shared.TransactionInfo) (shared.NapasEntity, error) {

	return shared.NapasEntity{}, nil
}
